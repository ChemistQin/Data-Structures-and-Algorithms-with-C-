//
//  main.cpp
//  DynamicMemory
//
//  Created by 覃思平 on 2016/12/13.
//  Copyright © 2016年 覃思平. All rights reserved.
//
// new, which allocates, and optionally initializes, an object in dynamic memory and returns a pointer to that object;
// delete, which takes a pointer to a dynamic object, destroys that object, and frees the associated memory.
//
// C++11 smart pointer: shared_ptr , unique_ptr , weak_ptr.

#include <iostream>
#include <list>
#include "StrBlob.h"


int main(int argc, const char * argv[]) {
 /*
    shared_ptr<string> p1;  // shared_ptr that can point at a string
    shared_ptr<list<int>> p2;  // shared_ptr that can point at a list of ints
    shared_ptr<int> p3 = make_shared<int>(42);
    shared_ptr<string> p4 = make_shared<string>("naive");
    shared_ptr<int> p5 = make_shared<int>();
    shared_ptr<string> p6 = make_shared<string>();
    
 */
    
    
    return 0;
}
